import { useEffect, useState } from 'react';

// Other existing code...

// Ensure to keep everything else the same, only modify the specified lines

// Remove duplicate non-export declaration
// function buildCoachPrompt(memberKey) {

export function buildCoachPrompt(memberKey) {
    // Function implementation...
}

// Other existing code...

// Remove stray closing brace
// Current last line closing brace should be commented or removed to maintain structure

// Other existing code continues...